# v0.0.72 to v0.0.73

- Make the compiled static site owned by the unprivileged runtime user and
  normalize static directories to `755` and files to `644`.
- Fail production startup when critical static assets cannot be read.
- Make `/health`, `/ready`, and Docker health status verify the service worker,
  icon, manifest, doodles, and Vite-hashed JavaScript/CSS assets.

No datastore migration or configuration change is required.
