# v0.0.29 to v0.0.30 Changelog

## Docker-only workflow docs

- Updated `AGENTS.md` so agents use Docker Compose for runtime, lint, and build
  commands instead of running Node or npm directly on the host.
- Synchronized README, architecture, deployment, and env example wording around
  Docker as the supported workflow.
- Added the required root `ARCHITECTURE.md` entrypoint and simplified
  `CLAUDE.md` to import `AGENTS.md`.
