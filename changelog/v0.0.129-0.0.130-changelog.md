# Drop Network 0.0.130

- Separated the published patient-side call contact from the verified request owner’s private account details.
- Made only the requester’s account phone require OTP verification; other call-contact numbers are clearly unverified.
- Automatically creates a minimal member account for a new verified requester inside the request workflow.
