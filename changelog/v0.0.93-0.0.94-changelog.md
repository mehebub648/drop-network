# Drop 0.0.94

- Compressed the donor-card phone and action area into a fixed-height footer instead of letting it fill unused card space.
- Kept the masked number and action on one row, with shorter labels on small screens.
- No data migration or configuration change is required.
