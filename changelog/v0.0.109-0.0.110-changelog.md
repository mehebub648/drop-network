# Drop Network 0.0.110

- Treats a blood-group-only directory link as an already completed choice, so
  current and older Android builds open directly on location selection.
- Removes the search progress strip and keeps only a quiet option to change the
  selected blood group.
