# v0.0.1 to v0.0.2 Changelog

- Added shared agent instructions in `AGENTS.md`.
- Added `CLAUDE.md` as a pointer to the shared agent instructions.
- Added `ARCHITECTURE.md` with the current app structure, API routes, storage model, deployment flow, and known limits.
- Added README links for architecture, agent instructions, and changelogs.
- Added rules that every future edit must update affected docs, bump the app version, and create a new changelog file.
- Updated the app version to `0.0.2`.
