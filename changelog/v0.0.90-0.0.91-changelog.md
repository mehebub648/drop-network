# Drop 0.0.91

- Fixed the registered-donor contact reveal path to pass the request's district and upazila context into preference matching.
- Updated the compatible transitive `ip-address` package to its patched release so the production dependency audit is clean.
- Restored a clean production-container TypeScript check for the release.
- Split the largest client-side framework, animation, icon, and date dependencies into stable vendor chunks so the production build no longer emits an oversized entry-bundle warning.
- No data migration or configuration change is required.
