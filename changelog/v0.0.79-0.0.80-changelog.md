# 0.0.79 to 0.0.80

- Replaced the separate call-report page with a non-dismissible global dialog
  that blocks the rest of Drop until the outcome is saved.
- Restored pending call reports after navigation, reloads, focus changes, and
  cross-tab updates without placing the revealed number in the URL.
- Enforced one unanswered reveal per account across all of its blood requests,
  and kept old call-page links as private redirects to donor search.

No migration or configuration change is required.
