# v0.0.3 to v0.0.4 Changelog

- Added a "rebuild `/profile` as a real multi-page area" feature plan to `PLAN.md`, covering nested routes (account, donor, requests, history, settings), account editing, donation-eligibility, and which sub-items depend on external services.
- Updated the app version to `0.0.4` across `package.json`, `package-lock.json`, `ARCHITECTURE.md`, and `README.md`.
