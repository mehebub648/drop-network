# v0.0.2 to v0.0.3 Changelog

- Added `PLAN.md` as the live project backlog, capturing correctness bugs, security hardening, external/credential-dependent work, and test-only items to remove before production.
- Added a "Plan File" section to `AGENTS.md` requiring agents to read `PLAN.md` before work, remove completed items, and add new findings.
- Updated the app version to `0.0.3` across `package.json`, `package-lock.json`, `ARCHITECTURE.md`, and `README.md`.
