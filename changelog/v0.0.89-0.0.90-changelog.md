# Drop 0.0.90

- Synchronized the production lockfile with PostCSS's supported Nano ID dependency so clean container installs succeed.
- No application behavior, data schema, or configuration changed.
