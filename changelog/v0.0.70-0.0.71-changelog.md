# v0.0.70 to v0.0.71

- Removed an unnecessary recursive ownership rewrite over the production
  application and dependency tree during Docker image creation.
- Kept ownership changes scoped to the writable LanceDB and community-media
  directories before the process drops to the unprivileged `node` user.

No API, datastore, configuration, or migration change is required.
