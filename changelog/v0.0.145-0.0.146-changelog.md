# 0.0.146

- Lets native registration submit a canonical district name while the backend
  resolves and stores its trusted coordinates.
- Keeps the existing browser registration location contract unchanged.
