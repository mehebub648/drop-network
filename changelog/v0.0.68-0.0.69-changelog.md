# v0.0.68 to v0.0.69

- Combined district and upazila into one guided location step.
- Let the landing illustration and search panel use the available desktop width
  without the previous fixed caps and oversized gap.
- Stacked the requester-role choices and summarize the saved choice in the
  request dialog, with an explicit Change action before publishing.
- Split patient, contact, review, and optional donor-registration details into
  shorter stages. Reopening a completed draft now starts at review instead of
  asking for the same answers again.
- Render every modal in a shared top-level portal above the sticky navigation,
  lock background scrolling, and keep overflow inside the dialog on desktop
  and mobile.
- Show saved search and patient/contact details as editable review cards, and
  omit stale hidden contact fields if the requester changes role.

No API, datastore, configuration, or migration change is required.
