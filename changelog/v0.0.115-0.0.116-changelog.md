# Drop Network 0.0.116

- Shows the chosen coordination contacts with tap-to-call actions while a blood
  request is active, then removes them from public responses when it closes.
- Replaces the misleading public zero-donor section with owner-only matches
  from the complete registered and attributed-directory search.
- Makes the request review and privacy copy clearly explain that the selected
  contact is public while the request is active.
