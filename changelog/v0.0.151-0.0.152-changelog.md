# 0.0.152 — guest-first request experience

- Publish consented patient-side requests before private account verification.
- Replace fingerprint ownership with hashed device credentials and automatic
  same-device account adoption; enforce Bangladesh date deadlines and lifetimes.
- Add phone-first guided enrollment, optional donor consent and explicit availability.
- Share accessible custom controls and short forms across browser workflows.
- Keep donor contact protections, closure history and private account data separate.
- Pin the patched query parser for the existing server dependency advisories.
- Keep dependency auditing separate from deterministic container installation.
- Add isolated container regression checks; release verification remains tracked
  in `docs/PLAN.md` until completed.
