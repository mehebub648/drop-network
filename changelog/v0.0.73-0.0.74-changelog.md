# Drop Network 0.0.74

- Enforce LF line endings for shell launchers so Linux containers built from a
  Windows checkout can execute `docker-entrypoint.sh`.
- No data or configuration migration is required; rebuild the production image
  to apply the corrected launcher.
