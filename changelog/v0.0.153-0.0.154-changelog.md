# v0.0.154

- Ignore unanswered calls from closed, expired or deleted requests when checking
  pending calls, so they cannot block donor contact on an active request.
- Preserve call history and require outcomes for unanswered calls on live requests.
- Extend regression coverage for pending-call selection.
