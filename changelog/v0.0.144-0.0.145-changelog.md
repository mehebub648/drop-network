# 0.0.145

- Add the backward-compatible `/api/v1` entry point for native clients.
- Accept revocable native sessions through bearer authentication while
  preserving secure cookie and origin protections for the website.
- Move the existing Flutter-native API reads onto the versioned contract.
