# Drop Network 0.0.103

- Show every donor search result's masked phone number directly below the
  donor name.
- Format masked numbers as visible prefix, spaced asterisks, and the final two
  digits, such as `+880171 * * * * * 78`.
