# v0.0.71 to v0.0.72

- Assign package metadata and server source to the unprivileged runtime user
  during Docker image creation.
- Prevent restrictive source-archive file modes from making the production
  process unable to read its application files.

No API, datastore, configuration, or migration change is required.
